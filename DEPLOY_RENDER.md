# Деплой на Render

Полная инструкция по деплою Cosmetic Agent на Render.

## Почему Render?

- ✅ Бесплатный тариф (Free tier)
- ✅ Простая настройка
- ✅ Автоматический деплой из GitHub
- ✅ Встроенные логи и мониторинг
- ✅ HTTPS из коробки

⚠️ **Важно**: На бесплатном тарифе сервис "засыпает" после 15 минут бездействия. Первый запрос после сна будет идти ~30-50 секунд.

## Шаг 1: Подготовка проекта

### 1.1 Создай GitHub репозиторий

1. Перейди на [github.com](https://github.com)
2. Создай новый репозиторий (например, `cosmetic-agent`)
3. Репозиторий может быть приватным или публичным

### 1.2 Залей код на GitHub

```bash
# Находясь в папке проекта
cd /Users/aleksandr/Desktop/MyGoogleScripts/cosmetic-agent

# Добавь GitHub remote
git remote add origin https://github.com/твой-username/cosmetic-agent.git

# Сделай первый коммит (если еще не сделал)
git add .
git commit -m "Initial commit: Cosmetic Agent setup"

# Залей на GitHub
git push -u origin main
```

Если просит main/master:
```bash
# Проверь текущую ветку
git branch

# Если ветка называется master, переименуй в main
git branch -M main
git push -u origin main
```

## Шаг 2: Создание Web Service на Render

### 2.1 Регистрация

1. Перейди на [render.com](https://render.com)
2. Зарегистрируйся или войди (можно через GitHub)

### 2.2 Создание Web Service

1. На дашборде нажми **"New +"** → **"Web Service"**
2. Подключи свой GitHub аккаунт (если еще не подключен)
3. Выбери репозиторий `cosmetic-agent`
4. Нажми **"Connect"**

### 2.3 Настройка сервиса

Заполни форму:

**Основные настройки:**
- **Name**: `cosmetic-agent` (или любое уникальное имя)
- **Region**: Выбери ближайший регион (например, Frankfurt для Европы)
- **Branch**: `main`
- **Root Directory**: оставь пустым
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`

**Тарифный план:**
- Выбери **"Free"** (0$/месяц)

**Переменные окружения** (Environment Variables):

Нажми **"Add Environment Variable"** и добавь:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `3000` |
| `GOOGLE_DRIVE_FOLDER_ID` | `1rELo6cM69I8_105PY-XKuJv1W33VDH1R` |
| `GOOGLE_SHEET_ID` | `твой_sheet_id` |
| `WEBHOOK_SECRET` | `my-secret-key-123` |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | Вставь содержимое `credentials.json` (см. ниже) |

### 2.4 Добавление Google Credentials

Открой файл `credentials.json` локально и скопируй **весь** JSON (должен быть одной строкой или можно минифицировать):

```json
{"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@....iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}
```

Вставь это значение в переменную `GOOGLE_SERVICE_ACCOUNT_JSON`.

⚠️ **Важно**:
- Render автоматически экранирует переносы строк в `private_key`
- Если возникнут проблемы, убедись, что `\n` в ключе сохранены

### 2.5 Запуск деплоя

1. Нажми **"Create Web Service"**
2. Render начнет сборку и деплой (займет 2-5 минут)
3. Следи за логами в реальном времени

## Шаг 3: Проверка деплоя

### 3.1 Получение URL

После успешного деплоя, Render даст тебе URL:

```
https://cosmetic-agent.onrender.com
```

### 3.2 Тест Health Check

Открой в браузере:

```
https://cosmetic-agent.onrender.com/health
```

Должен вернуться JSON:

```json
{
  "status": "ok",
  "timestamp": "2024-01-18T12:00:00.000Z",
  "uptime": 123.456,
  "environment": "production"
}
```

### 3.3 Тест Webhook

Используй curl или Postman:

```bash
curl -X POST https://cosmetic-agent.onrender.com/webhook \
  -H "Content-Type: application/json" \
  -H "x-webhook-secret: my-secret-key-123" \
  -d '{
    "chatId": "test-123",
    "productName": "Тестовый продукт",
    "purpose": "Тестирование",
    "application": "Тест",
    "inci": "Aqua, Glycerin",
    "inciDocUrl": "https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf",
    "photoUrls": ["https://via.placeholder.com/300.jpg"]
  }'
```

## Шаг 4: Настройка n8n

### 4.1 Обновление Webhook URL в n8n

В твоем n8n workflow:

1. Найди HTTP Request node
2. Измени URL на Render URL:

```
URL: https://cosmetic-agent.onrender.com/webhook
```

3. Добавь header:

```
x-webhook-secret: my-secret-key-123
```

### 4.2 Тестирование

Отправь тестовое сообщение в Telegram бота и проверь:

1. Логи в Render (должны появиться записи)
2. Google Drive (должна создаться папка)
3. Google Sheets (должна добавиться строка)

## Шаг 5: Мониторинг и логи

### 5.1 Просмотр логов

1. В дашборде Render, открой твой сервис
2. Перейди на вкладку **"Logs"**
3. Логи обновляются в реальном времени

### 5.2 Метрики

На вкладке **"Metrics"** можно увидеть:
- CPU usage
- Memory usage
- HTTP requests

### 5.3 События

На вкладке **"Events"** видны:
- Деплои
- Перезапуски
- Ошибки

## Обновление кода

### Автоматический деплой

Render настроен на автоматический деплой при пуше в GitHub:

```bash
# Внеси изменения в код
# ...

# Закоммить
git add .
git commit -m "Update: описание изменений"

# Залей на GitHub
git push origin main

# Render автоматически задеплоит новую версию
```

### Ручной деплой

В дашборде Render:
1. Нажми **"Manual Deploy"**
2. Выбери ветку
3. Нажми **"Deploy"**

## Полезные команды

### Локальное тестирование перед деплоем

```bash
# Запусти локально в production режиме
NODE_ENV=production npm start
```

### Проверка переменных окружения

В Render дашборде:
1. Перейди в **"Environment"**
2. Проверь все переменные
3. Можно изменить и сервис автоматически перезапустится

## Решение проблем

### Проблема: "Application failed to respond"

**Причины:**
1. Сервер не слушает правильный порт
2. Ошибка в коде при старте

**Решение:**
1. Проверь логи в Render
2. Убедись, что в коде используется `process.env.PORT`
3. Проверь, что в `package.json` правильный `start` скрипт

### Проблема: "Google API error 403"

**Причины:**
1. Service Account не имеет доступа к Drive/Sheets
2. Неправильный формат credentials

**Решение:**
1. Проверь, что Service Account email добавлен в права доступа
2. Проверь переменную `GOOGLE_SERVICE_ACCOUNT_JSON` в Render
3. Убедись, что Google Drive API и Sheets API включены

### Проблема: Сервис "засыпает"

**Это нормально** на бесплатном тарифе Render. Сервис засыпает после 15 минут бездействия.

**Решения:**
1. **Upgrade на платный тариф** (~$7/месяц) - сервис не будет засыпать
2. **Использовать пинг-сервис** (например, [UptimeRobot](https://uptimerobot.com/)) - каждые 5 минут пингует `/health`
3. **Принять это** - первый запрос будет медленным, но последующие быстрые

### Настройка UptimeRobot (бесплатно)

1. Зарегистрируйся на [uptimerobot.com](https://uptimerobot.com)
2. Создай новый монитор:
   - Type: HTTP(s)
   - URL: `https://cosmetic-agent.onrender.com/health`
   - Monitoring Interval: 5 minutes
3. Теперь сервис не будет засыпать

## Переход на Railway (опционально)

Если Render не устраивает (засыпание), можно использовать Railway:

1. Перейди на [railway.app](https://railway.app)
2. Залогинься через GitHub
3. Нажми "New Project" → "Deploy from GitHub repo"
4. Выбери репозиторий `cosmetic-agent`
5. Добавь те же переменные окружения
6. Railway даст $5 бесплатных кредитов

Плюсы Railway:
- Не засыпает
- Быстрее деплой
- Лучшие логи

Минусы:
- Платный (~$5/месяц после бесплатных кредитов)

## Checklist деплоя

- [ ] Код залит на GitHub
- [ ] Создан Web Service на Render
- [ ] Все переменные окружения настроены
- [ ] Google credentials добавлены
- [ ] Деплой успешен (статус "Live")
- [ ] `/health` отвечает успешно
- [ ] `/webhook` работает (тест через curl)
- [ ] n8n настроен на новый URL
- [ ] Тест end-to-end через Telegram прошел успешно
- [ ] (Опционально) Настроен UptimeRobot

## Готово!

Теперь твой агент работает в облаке 24/7 и готов принимать запросы от n8n.

URL агента: `https://cosmetic-agent.onrender.com`

Удачи! 🚀
