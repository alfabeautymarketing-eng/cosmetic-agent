# Статус реализации системы создания карточек продуктов

**Дата последнего обновления:** 2025-11-26
**Версия:** Backend Foundation Complete (Phases 1-3)

---

## ✅ ЗАВЕРШЕНО

### Phase 1: Backend Foundation (100%)

#### 1.1 Новый формат Card ID
**Файлы:** `src/services/cardProcessor.js`

**Изменения:**
- Старый формат: `CARD-U2025_11_26_WF-0001-C0001`
- Новый формат: `C-U2025_11_26_WF-0001-0001`
- Обновлен метод `generateCardIdWithUser()`
- Обновлен метод `getUserCardCount()` для работы с новым форматом

**Тестирование:** ✅ Логика протестирована

---

#### 1.2 Иерархическая структура папок
**Файлы:**
- `src/services/driveService.js` (4 новых метода)
- `src/services/userService.js` (автосоздание папки при регистрации)

**Новая структура:**
```
/GOOGLE_DRIVE_FOLDER_ID/
  /U2025_11_26_WF-0001/              ← создается при регистрации
    /C-U2025_11_26_WF-0001-0001 Название продукта/  ← создается при "Создать карточку"
      Этикетка Название.pdf
      INCI Название.pdf
      /Фото/                          ← создается автоматически
        photo1.jpg
        photo2.jpg
```

**Новые методы driveService:**
1. `createUserFolder(userId)` - создает/находит папку пользователя
2. `createCardFolder(cardFolderName, userFolderId)` - создает папку карточки
3. `createPhotosFolder(cardFolderId)` - создает подпапку Фото
4. `renameFolder(folderId, newName)` - переименовывает папку

**Тестирование:** ⚠️ Требуется тестирование с реальным Google Drive

---

#### 1.3 Обновление схемы Google Sheets
**Файлы:** `src/services/sheetsService.js`

**Структура изменилась с 19 колонок (A-S) на 21 колонку (A-U):**

| Колонка | Старая | Новая | Описание |
|---------|--------|-------|----------|
| A | ID | ID | Автоинкремент |
| B | ID-Карточки | ID-Карточки | Card ID |
| C | ID-Чат | ID-Чат | User ID |
| D | Наименование | Наименование | Название продукта |
| E | Назначение | **Этикетка ссылка** | 🆕 Ссылка на файл этикетки |
| F | Применение | **Этикетки инфа** | 🆕 AI-извлеченная информация |
| G | ИНСИ | Назначение | Сдвиг с E |
| H | ИНСИ док | Применение | Сдвиг с F |
| I | AI поле 1 | ИНСИ | Сдвиг с G |
| J | AI поле 2 | ИНСИ док | Сдвиг с H |
| K-P | AI поля 3-8 | AI поля (активные, состав) | Сдвиг с I-N |
| Q-U | Коды | Коды (ТН ВЭД, категории) | Сдвиг с O-S |

**Обновленные методы:**
- `addCardRow()` - добавляет 21 колонку
- `getRowByCardId()` - читает A:U
- `getRows()` - читает A:U
- `updateRowWithAiData()` - обновляет K:P (было I:N)

**Новые методы:**
- `updateLabelInfo(rowNumber, labelLink, labelInfo)` - обновляет E:F
- `updateProductName(rowNumber, newName)` - обновляет D

**Тестирование:** ⚠️ Требуется тестирование с реальной Google Sheets

---

### Phase 2: Multi-Stage API Endpoints (100%)

**Файл:** `src/routes/cards.js` (НОВЫЙ, 450+ строк)
**Регистрация:** `src/server.js` - добавлен роутер `/api/cards`

**6 новых endpoints:**

#### 1. `POST /api/cards/create` - Stage 1
**Назначение:** Создание карточки с базовой информацией

**Требует:** JWT авторизации (`authMiddleware`)

**Входные данные:**
```json
{
  "productName": "Название продукта",
  "purpose": "Назначение",
  "application": "Применение"
}
```

**Действия:**
1. Генерирует Card ID: `C-{UserID}-{номер}`
2. Создает папку пользователя (если не существует)
3. Создает папку карточки: `{CardID} {Название}`
4. Создает подпапку `/Фото/`
5. Добавляет строку в Google Sheets (частичные данные)

**Ответ:**
```json
{
  "success": true,
  "cardId": "C-U2025_11_26_WF-0001-0001",
  "cardFolderId": "google_drive_folder_id",
  "userFolderId": "user_folder_id",
  "photosFolderId": "photos_folder_id",
  "sheetRow": 5,
  "folderUrl": "https://drive.google.com/drive/folders/...",
  "message": "Карточка создана. Теперь можно загрузить этикетку."
}
```

**Тестирование:** ⚠️ Требуется API тестирование

---

#### 2. `POST /api/cards/:cardId/label` - Stage 2
**Назначение:** Загрузка и AI-обработка этикетки

**Требует:** JWT авторизации + `multipart/form-data`

**Входные данные:**
- `labelFile` - файл (PDF или изображение)
- `cardFolderId` - ID папки карточки
- `productName` - название продукта

**Действия:**
1. Загружает файл в Drive: `Этикетка {Название}.{ext}`
2. Извлекает текст из PDF (если применимо)
3. Обрабатывает через `aiService.analyzeLabelOnly()`
4. Обновляет колонки E и F в Sheets

**AI анализ этикетки:**
- Извлекает информацию: производитель, объем, срок годности, сертификация
- Предлагает назначение продукта
- Предлагает способ применения

**Ответ:**
```json
{
  "success": true,
  "labelLink": "https://drive.google.com/file/d/...",
  "labelFileName": "Этикетка Название.pdf",
  "aiSuggestions": {
    "purpose": "Предложенное назначение",
    "application": "Предложенное применение"
  },
  "labelInfo": "Извлеченная информация с этикетки",
  "message": "Этикетка загружена и обработана AI"
}
```

**Тестирование:** ⚠️ Требуется API тестирование + Gemini API ключ

---

#### 3. `POST /api/cards/:cardId/inci` - Stage 3
**Назначение:** Загрузка и AI-обработка INCI состава

**Требует:** JWT авторизации + `multipart/form-data`

**Входные данные:**
- `inciFile` - файл (PDF или изображение)
- `cardFolderId` - ID папки карточки
- `productName` - название продукта
- `purpose` - назначение продукта

**Действия:**
1. Загружает файл в Drive: `INCI {Название}.{ext}`
2. Извлекает текст из PDF
3. Обрабатывает через `aiService.analyzeProduct()`
4. Обновляет колонки I, J (INCI текст и ссылка)
5. Обновляет колонки K-P (AI результаты)

**AI анализ INCI:**
- Полный состав (RU без %, EN без %)
- Активные ингредиенты (RU, EN)
- Описание для буклета (RU, EN)

**Ответ:**
```json
{
  "success": true,
  "inciLink": "https://drive.google.com/file/d/...",
  "inciFileName": "INCI Название.pdf",
  "aiResults": {
    "fullComposition": { "ru": "...", "en": "..." },
    "activeIngredients": { "ru": [...], "en": [...] },
    "bookletComposition": { "ru": "...", "en": "..." }
  },
  "message": "INCI загружен и обработан AI"
}
```

**Тестирование:** ⚠️ Требуется API тестирование + Gemini API ключ

---

#### 4. `POST /api/cards/:cardId/photos` - Stage 4
**Назначение:** Загрузка фотографий продукта

**Требует:** JWT авторизации + `multipart/form-data`

**Входные данные:**
- `photos` - массив файлов (до 10 изображений)
- `photosFolderId` - ID папки Фото

**Действия:**
1. Загружает каждое фото в папку `/Фото/`
2. Сохраняет оригинальные имена файлов

**Ответ:**
```json
{
  "success": true,
  "uploadedPhotos": [
    { "name": "photo1.jpg", "id": "...", "url": "..." },
    { "name": "photo2.jpg", "id": "...", "url": "..." }
  ],
  "count": 2,
  "message": "Загружено 2 фото"
}
```

**Тестирование:** ⚠️ Требуется API тестирование

---

#### 5. `PATCH /api/cards/:cardId/name`
**Назначение:** Редактирование названия продукта

**Требует:** JWT авторизации

**Входные данные:**
```json
{
  "newName": "Новое название",
  "cardFolderId": "folder_id"
}
```

**Действия:**
1. Обновляет колонку D в Sheets
2. Переименовывает папку в Drive: `{CardID} {Новое название}`

**Ответ:**
```json
{
  "success": true,
  "newName": "Новое название",
  "newFolderName": "C-U2025_11_26_WF-0001-0001 Новое название",
  "message": "Название обновлено"
}
```

**Тестирование:** ⚠️ Требуется API тестирование

---

#### 6. `POST /api/cards/:cardId/feedback`
**Назначение:** Отправка обратной связи по AI результатам

**Требует:** JWT авторизации

**Входные данные:**
```json
{
  "resultType": "label|inci|fullComposition|activeIngredients|bookletComposition",
  "feedback": "Текст обратной связи",
  "corrections": "Предложенные исправления"
}
```

**Действия:**
1. Логирует обратную связь в консоль
2. TODO: Сохранение в отдельную таблицу для памяти чатов

**Ответ:**
```json
{
  "success": true,
  "message": "Обратная связь принята. Спасибо!"
}
```

**Тестирование:** ⚠️ Требуется реализация хранения

---

### Phase 3: Enhanced AI Service (100%)

**Файл:** `src/services/aiService.js`

**Модель:** `gemini-1.5-pro`

**2 новых метода:**

#### 1. `analyzeLabelOnly(data, imageBuffer, mimeType)`
**Назначение:** Анализ только этикетки

**Входные данные:**
- `data.productName` - название продукта
- `data.labelText` - извлеченный текст (опционально)
- `imageBuffer` - изображение этикетки
- `mimeType` - тип файла

**Возвращает:**
```json
{
  "labelInfo": "Краткое описание информации с этикетки (2-3 предложения)",
  "suggestedPurpose": "Предложенное назначение",
  "suggestedApplication": "Предложенный способ применения"
}
```

**Промпт фокусируется на:**
- Производитель, объем, срок годности
- Ключевые активные ингредиенты на передней этикетке
- Сертификации (organic, vegan, cruelty-free)
- Инструкции по применению

**Fallback:** Если нет GEMINI_API_KEY, возвращает сообщение об ошибке

---

#### 2. `analyzeInciEnhanced(data, imageBuffer, mimeType)`
**Назначение:** Расширенный анализ INCI с извлечением процентов

**Входные данные:**
- `data.productName` - название продукта
- `data.purpose` - назначение
- `data.inci` - текст INCI
- `imageBuffer` - изображение документа
- `mimeType` - тип файла

**Возвращает:**
```json
{
  "fullComposition": "Полный состав RU без %",
  "fullCompositionEn": "Full composition EN without %",
  "fullCompositionWithPercent": "Полный состав RU с % (если есть)",
  "fullCompositionEnWithPercent": "Full composition EN with % (if available)",
  "activeIngredients": ["Активный ингредиент 1", "..."],
  "activeIngredientsEn": ["Active ingredient 1", "..."],
  "bookletComposition": "Маркетинговое описание (RU)",
  "bookletCompositionEn": "Marketing description (EN)"
}
```

**Ключевые правила:**
1. Если INCI содержит проценты - извлечь их
2. Если процентов нет - поля "WithPercent" остаются пустыми
3. Активные ингредиенты = только те, что имеют косметический эффект (не вода, консерванты, эмульгаторы)
4. Точный перевод названий между RU и EN
5. Описание для буклета - маркетинговое, подчеркивает пользу

**Fallback:** Если нет GEMINI_API_KEY, возвращает mock данные

---

## ⚠️ НЕ ЗАВЕРШЕНО

### Phase 4: Frontend Restructuring (0%)

**Требуется создать новый UI с прогрессивным раскрытием (progressive disclosure)**

#### Структура формы (5 блоков):

**Блок 1: Информация о продукте**
- Поля: Наименование, Назначение, Применение
- Кнопка: "Создать карточку"
- Остальные блоки скрыты до создания карточки

**Блок 2: Этикетка** (появляется после создания карточки)
- 4 кнопки загрузки: Файл | Ссылка | Вставить из буфера | Текст
- Убрать текст "Нажмите для загрузки..."
- Автоматическая AI обработка при загрузке
- Отображение результатов AI:
  - Извлеченная информация
  - Предложения по назначению/применению

**Блок 3: INCI состав** (появляется после этикетки)
- 4 кнопки загрузки: Файл | Ссылка | Вставить из буфера | Текст
- Кнопка "Отправить" для запуска AI
- 3 блока результатов (появляются после обработки):
  1. Полный состав (табы: RU без %, RU с %, EN без %, EN с %)
  2. Активные ингредиенты (табы: RU, EN)
  3. Описание для буклета (табы: RU, EN)
- Кнопки обратной связи на каждом блоке результатов

**Блок 4: Фотографии** (появляется после INCI)
- 3 кнопки: Файл | Ссылка | Вставить из буфера
- Предпросмотр загруженных фото
- Кнопка "Загрузить фото"

**Блок 5: Коды и категории** (появляется после фото)
- Будет реализовано позже

#### Файлы для изменения:
- `public/index.html` - полная переработка формы
- `public/app.js` - НОВЫЙ файл с логикой многоэтапной формы
- `public/auth.js` - интеграция с новой формой

#### Ключевые особенности:
1. **Прогрессивное раскрытие:** Блоки появляются по мере выполнения шагов
2. **Состояние приложения:** Хранить `currentCard` с ID и папками
3. **Вставка из буфера:** Реализовать Ctrl+V для скриншотов
4. **Табы результатов:** Переключение между языками и вариантами
5. **Загрузка:** Показывать индикаторы во время AI обработки
6. **Обратная связь:** Модальное окно для комментариев к AI результатам

---

### Phase 5: Product Name Editing (0%)

**UI компонент для редактирования:**
- Кнопка "✏️ Редактировать" рядом с полем наименования
- После клика - поле становится редактируемым
- Кнопка "💾 Сохранить"
- При сохранении - вызов `PATCH /api/cards/:cardId/name`

**Уже реализовано в backend:**
- ✅ API endpoint готов
- ✅ Обновление Sheets готово
- ✅ Переименование папки готово

**Требуется:**
- Frontend UI
- JavaScript логика

---

### Phase 6: Testing & Deployment (0%)

#### Чек-лист тестирования:

**Backend API тесты (Postman/curl):**
- [ ] Регистрация нового пользователя → проверка папки в Drive
- [ ] POST /api/cards/create → проверка Card ID, папок, Sheets
- [ ] POST /api/cards/:cardId/label → проверка загрузки файла, AI обработки, Sheets E-F
- [ ] POST /api/cards/:cardId/inci → проверка загрузки файла, AI обработки, Sheets I-J, K-P
- [ ] POST /api/cards/:cardId/photos → проверка загрузки в папку /Фото/
- [ ] PATCH /api/cards/:cardId/name → проверка Sheets и Drive rename

**Frontend тесты (браузер):**
- [ ] Блок 1: Создание карточки → появление Блока 2
- [ ] Блок 2: Загрузка этикетки → AI результаты → появление Блока 3
- [ ] Блок 3: Загрузка INCI → AI результаты → 3 блока → появление Блока 4
- [ ] Блок 4: Загрузка фото → появление Блока 5
- [ ] Редактирование названия → обновление везде
- [ ] Вставка из буфера (Ctrl+V) для этикетки, INCI, фото
- [ ] Табы переключения языков в результатах
- [ ] Форма обратной связи

**Интеграционные тесты:**
- [ ] Полный цикл: регистрация → создание карточки → этикетка → INCI → фото
- [ ] Проверка Google Sheets: все 21 колонка заполнены корректно
- [ ] Проверка Google Drive: структура папок соответствует ожидаемой
- [ ] AI результаты: корректность извлечения и форматирования

---

## 📋 ТРЕБУЕТСЯ ДЛЯ ЗАПУСКА

### Environment Variables (.env)

```bash
# Google APIs
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./credentials.json
GOOGLE_DRIVE_FOLDER_ID=your_root_folder_id
GOOGLE_SHEET_ID=your_spreadsheet_id

# AI
GEMINI_API_KEY=your_gemini_api_key  # ⚠️ ОБЯЗАТЕЛЬНО для Phase 2-3

# Auth
JWT_SECRET=cosmetic-agent-jwt-secret-change-in-production

# Server
PORT=3000
NODE_ENV=development
```

### Получение Gemini API ключа

1. Перейти на https://makersuite.google.com/app/apikey
2. Создать новый проект (если нужно)
3. Нажать "Create API Key"
4. Скопировать ключ и добавить в `.env` как `GEMINI_API_KEY=...`

**Важно:** Без этого ключа AI функции будут возвращать mock данные.

---

## 🚀 ЗАПУСК ТЕКУЩЕЙ ВЕРСИИ

### 1. Подготовка Google Sheets

**Нужно добавить 2 новые колонки вручную:**

1. Открыть таблицу "Карточки"
2. Вставить 2 пустые колонки после D (Наименование):
   - Колонка E: "Этикетка ссылка"
   - Колонка F: "Этикетки инфа"
3. Все существующие колонки сдвинутся автоматически

**Или:** Создать новую таблицу с нуля с 21 колонкой.

### 2. Перезапуск сервера

```bash
# Остановить текущий процесс (если запущен)
lsof -ti:3000 | xargs kill -9

# Запустить обновленный сервер
node src/server.js
```

### 3. Проверка логов

При запуске должны быть видны:
```
🚀 Cosmetic Agent running on port 3000
📁 Google Drive Folder: 1rELo6cM69I8_105PY-XKuJv1W33VDH1R
📊 Google Sheet ID: 1JMzIi2-qNBwZsBqwDhOdJTocNjZ7BsdSXWs7cAKf3oU
🌍 Environment: development
⚠️ GEMINI_API_KEY is not set. AI features will be disabled.  # ← если нет ключа
```

---

## 📝 ПРИМЕРЫ API ТЕСТОВ

### Пример 1: Создание карточки

```bash
# 1. Регистрация и вход (получить токен)
TOKEN="your_jwt_token_here"

# 2. Создать карточку
curl -X POST http://localhost:3000/api/cards/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "productName": "Крем для лица увлажняющий",
    "purpose": "Интенсивное увлажнение кожи лица",
    "application": "Наносить на очищенную кожу утром и вечером"
  }'

# Ответ (сохранить cardId, cardFolderId, photosFolderId):
{
  "success": true,
  "cardId": "C-U2025_11_26_WF-0001-0001",
  "cardFolderId": "folder_id_here",
  "photosFolderId": "photos_folder_id_here",
  ...
}
```

### Пример 2: Загрузка этикетки

```bash
curl -X POST http://localhost:3000/api/cards/C-U2025_11_26_WF-0001-0001/label \
  -H "Authorization: Bearer $TOKEN" \
  -F "labelFile=@/path/to/label.pdf" \
  -F "cardFolderId=folder_id_from_step1" \
  -F "productName=Крем для лица увлажняющий"
```

### Пример 3: Загрузка INCI

```bash
curl -X POST http://localhost:3000/api/cards/C-U2025_11_26_WF-0001-0001/inci \
  -H "Authorization: Bearer $TOKEN" \
  -F "inciFile=@/path/to/inci.pdf" \
  -F "cardFolderId=folder_id_from_step1" \
  -F "productName=Крем для лица увлажняющий" \
  -F "purpose=Интенсивное увлажнение кожи лица"
```

### Пример 4: Загрузка фото

```bash
curl -X POST http://localhost:3000/api/cards/C-U2025_11_26_WF-0001-0001/photos \
  -H "Authorization: Bearer $TOKEN" \
  -F "photos=@/path/to/photo1.jpg" \
  -F "photos=@/path/to/photo2.jpg" \
  -F "photosFolderId=photos_folder_id_from_step1"
```

---

## 🔍 ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ

1. **Google OAuth** - только заглушка, не работает
2. **Telegram Bot** - только заглушка, не работает
3. **Email отправка** - коды только в консоль (не реальная отправка)
4. **Verification codes** - хранятся в памяти (при перезапуске теряются)
5. **Feedback хранение** - только логирование, нет постоянного хранения
6. **Frontend** - старый UI, не соответствует новому API

---

## 📁 СТРУКТУРА ПРОЕКТА (актуальная)

```
cosmetic-agent/
├── src/
│   ├── routes/
│   │   ├── auth.js          ✅ JWT авторизация
│   │   ├── cards.js         ✅ Новый multi-stage API
│   │   ├── form.js          ⚠️ Устаревший, будет удален
│   │   ├── health.js        ✅
│   │   └── webhook.js       ✅
│   ├── services/
│   │   ├── aiService.js     ✅ analyzeLabelOnly, analyzeInciEnhanced
│   │   ├── cardProcessor.js ✅ Новый формат Card ID
│   │   ├── driveService.js  ✅ Иерархические папки
│   │   ├── sheetsService.js ✅ 21 колонка A-U
│   │   └── userService.js   ✅ Автосоздание папок
│   └── server.js            ✅ Роутер /api/cards
├── public/
│   ├── index.html           ⚠️ Старый UI, требует переработки
│   ├── auth.js              ✅ JWT клиент
│   └── app.js               ❌ Не создан (нужен для Phase 4)
├── .env                     ⚠️ Нужен GEMINI_API_KEY
└── IMPLEMENTATION_STATUS.md ✅ Этот файл
```

---

## 🎯 ПРИОРИТЕТЫ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ

### Высокий приоритет:
1. ✅ **Получить Gemini API ключ** - без него AI не работает
2. ✅ **Обновить Google Sheets** - добавить колонки E и F
3. ✅ **Протестировать backend API** - хотя бы создание карточки

### Средний приоритет:
4. **Phase 4: Frontend restructuring** - основная работа
5. **Phase 5: Name editing UI** - простая задача

### Низкий приоритет:
6. **Phase 6: Comprehensive testing** - после завершения UI
7. **Feedback storage** - улучшение для production

---

## ❓ ВОПРОСЫ / ПРОБЛЕМЫ

### Для пользователя:
- [ ] Получен ли Gemini API ключ?
- [ ] Обновлена ли структура Google Sheets (добавлены колонки E и F)?
- [ ] Нужно ли сохранить старый endpoint `/api/create-card` или можно удалить?

### Технические:
- [ ] Как хранить feedback для памяти чатов? (отдельная таблица / база данных)
- [ ] Нужна ли валидация размера файлов на фронтенде до загрузки?
- [ ] Какой UX для длительной AI обработки? (прогресс-бар / индикатор)

---

**Следующий шаг:** Phase 4 - Frontend Restructuring
**Ответственный файл:** `public/index.html` и новый `public/app.js`
**Ориентировочное время:** 3-4 часа разработки
