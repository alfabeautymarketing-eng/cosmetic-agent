# Инструкция для продолжения в новой сессии

**Дата:** 2025-11-26
**Текущий этап:** Phase 4 - Frontend Restructuring

---

## ✅ ЧТО УЖЕ СДЕЛАНО (кратко)

### Backend (100% готов)
- ✅ Новый формат Card ID: `C-U2025_11_26_WF-0001-0001`
- ✅ Иерархия папок: `/UserID/CardID Название/Фото/`
- ✅ Google Sheets: 21 колонка (A-U) вместо 19
- ✅ 6 API endpoints в `src/routes/cards.js`:
  - POST /api/cards/create
  - POST /api/cards/:cardId/label
  - POST /api/cards/:cardId/inci
  - POST /api/cards/:cardId/photos
  - PATCH /api/cards/:cardId/name
  - POST /api/cards/:cardId/feedback
- ✅ AI сервис: `analyzeLabelOnly()` и `analyzeInciEnhanced()`

---

## ⚠️ ЧТО НУЖНО СДЕЛАТЬ

### До начала работы:

1. **Получить Gemini API ключ:**
   - https://makersuite.google.com/app/apikey
   - Добавить в `.env`: `GEMINI_API_KEY=your_key`

2. **Обновить Google Sheets вручную:**
   - Открыть лист "Карточки"
   - Вставить 2 пустые колонки после D (Наименование):
     - E: "Этикетка ссылка"
     - F: "Этикетки инфа"

3. **Перезапустить сервер:**
   ```bash
   lsof -ti:3000 | xargs kill -9
   node src/server.js
   ```

### Основная задача: Phase 4 - Frontend

**Цель:** Переделать `public/index.html` и создать `public/app.js` для работы с новым API.

**Что делать:**

1. **Удалить поля Email и Имя из формы** (они теперь в шапке после авторизации)

2. **Создать 5 блоков с прогрессивным раскрытием:**

   **Блок 1:** Информация о продукте
   - Поля: Наименование, Назначение, Применение
   - Кнопка "Создать карточку"
   - Вызов: `POST /api/cards/create`
   - После успеха: показать Блок 2

   **Блок 2:** Этикетка (скрыт до создания карточки)
   - 4 кнопки: Файл | Ссылка | Вставить из буфера | Текст
   - Автозагрузка при выборе файла
   - Вызов: `POST /api/cards/:cardId/label`
   - Показать AI результаты (labelInfo, suggestions)
   - После успеха: показать Блок 3

   **Блок 3:** INCI состав
   - 4 кнопки: Файл | Ссылка | Вставить из буфера | Текст
   - Кнопка "Отправить"
   - Вызов: `POST /api/cards/:cardId/inci`
   - Показать 3 блока результатов:
     1. Полный состав (табы: RU без%, RU с%, EN без%, EN с%)
     2. Активные ингредиенты (табы: RU, EN)
     3. Описание для буклета (табы: RU, EN)
   - После успеха: показать Блок 4

   **Блок 4:** Фотографии
   - 3 кнопки: Файл | Ссылка | Вставить из буфера
   - Предпросмотр
   - Кнопка "Загрузить фото"
   - Вызов: `POST /api/cards/:cardId/photos`
   - После успеха: показать Блок 5

   **Блок 5:** Коды и категории (заглушка)

3. **Реализовать "Вставить из буфера":**
   - Ctrl+V для вставки скриншотов
   - Обработка `clipboardData.items`
   - Создание File объекта из blob

4. **Хранение состояния карточки:**
   ```javascript
   let currentCard = {
     cardId: null,
     cardFolderId: null,
     photosFolderId: null,
     productName: null
   };
   ```

5. **Обработка ошибок:**
   - 401 Unauthorized → открыть модальное окно входа
   - Другие ошибки → показать сообщение

---

## 📂 КЛЮЧЕВЫЕ ФАЙЛЫ

### Для чтения (понять текущее состояние):
- `IMPLEMENTATION_STATUS.md` - полная документация (17+ страниц)
- `src/routes/cards.js` - новый API (примеры запросов/ответов)
- `src/services/aiService.js` - методы AI обработки

### Для изменения:
- `public/index.html` - переделать форму (удалить старое, добавить 5 блоков)
- `public/app.js` - создать новый файл с логикой

### Не трогать:
- Весь backend (уже готов)
- `public/auth.js` (авторизация работает)

---

## 🧪 КАК ТЕСТИРОВАТЬ

### 1. Простой тест через API (без фронтенда):

```bash
# Получить токен (войти через браузер или API)
TOKEN="your_token_here"

# Создать карточку
curl -X POST http://localhost:3000/api/cards/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "productName": "Тестовый крем",
    "purpose": "Увлажнение",
    "application": "Наносить утром"
  }'

# Посмотреть в Google Drive и Sheets - должны появиться папки и строка
```

### 2. Тест через браузер (после фронтенда):

1. Открыть http://localhost:3000
2. Войти или зарегистрироваться
3. Заполнить Блок 1 → нажать "Создать карточку"
4. Проверить, что Блок 2 появился
5. Загрузить этикетку → проверить AI результаты
6. Загрузить INCI → проверить 3 блока результатов
7. Загрузить фото → проверить в Drive папке /Фото/

---

## 💡 ПОДСКАЗКИ ДЛЯ AI АССИСТЕНТА

Если продолжаешь в новой сессии, вот что важно знать:

**Контекст:**
- Система создания карточек косметических продуктов
- Многоэтапная форма (4 шага: инфо → этикетка → INCI → фото)
- Backend полностью готов, нужен только frontend

**Архитектура:**
- Backend: Express.js + Google Sheets API + Google Drive API + Gemini AI
- Frontend: Vanilla JS (без фреймворков)
- Auth: JWT токены в localStorage

**Ключевые особенности:**
1. Прогрессивное раскрытие: блоки появляются последовательно
2. AI обработка: этикетка автоматически, INCI по кнопке
3. Вставка из буфера: Ctrl+V для скриншотов
4. Переключение языков: табы RU/EN в результатах

**Что уже работает:**
- ✅ Авторизация (JWT)
- ✅ Backend API (все 6 endpoints)
- ✅ AI обработка (методы готовы)
- ✅ Google Sheets (структура обновлена)
- ✅ Google Drive (папки создаются)

**Что нужно сделать:**
- ❌ Переделать HTML форму
- ❌ Создать JavaScript для работы с API
- ❌ Реализовать прогрессивное раскрытие
- ❌ Добавить вставку из буфера
- ❌ Добавить переключение табов

**Стиль кода:**
- Не использовать фреймворки (только vanilla JS)
- async/await для всех API вызовов
- Четкие имена функций (createCard, uploadLabel, uploadInci)
- Комментарии на русском
- Обработка ошибок везде

---

## 🎯 EXPECTED OUTCOME

После Phase 4 у пользователя должна быть:

1. **Форма из 5 блоков**, где каждый появляется после выполнения предыдущего
2. **Работающая интеграция** с backend API (все 6 endpoints)
3. **AI результаты** отображаются в 3 блоках с переключением языков
4. **Загрузка файлов** работает (файл, ссылка, буфер обмена)
5. **Редактирование названия** после создания карточки
6. **Обратная связь** по AI результатам (кнопки на каждом блоке)

**UX поток:**
```
Авторизация
  ↓
Блок 1: Создать карточку → Success → Сохранить cardId, folderId
  ↓
Блок 2: Загрузить этикетку → AI обработка → Показать результаты
  ↓
Блок 3: Загрузить INCI → AI обработка → Показать 3 блока результатов
  ↓
Блок 4: Загрузить фото → Предпросмотр → Загрузка
  ↓
Блок 5: (заглушка)
```

---

## 🔗 ССЫЛКИ

- **Полная документация:** `IMPLEMENTATION_STATUS.md` (в этой же папке)
- **План разработки:** `.claude/plans/greedy-bubbling-scone.md`
- **Исходники backend:** `src/routes/cards.js` (450+ строк с комментариями)
- **AI сервис:** `src/services/aiService.js` (методы analyzeLabelOnly, analyzeInciEnhanced)

---

**Начни с чтения IMPLEMENTATION_STATUS.md → там все детали!**
