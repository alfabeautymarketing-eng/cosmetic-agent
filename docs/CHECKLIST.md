# ✅ Чек-лист перед продолжением

## Подготовка (сделать ДО новой сессии)

### 1. Получить Gemini API ключ
- [ ] Перейти на https://makersuite.google.com/app/apikey
- [ ] Войти под своим Google аккаунтом
- [ ] Создать новый API ключ
- [ ] Добавить в `.env` файл: `GEMINI_API_KEY=ваш_ключ_здесь`

**Важно:** Без этого ключа AI будет возвращать mock данные!

---

### 2. Обновить Google Sheets вручную
- [ ] Открыть таблицу "Карточки" в Google Sheets
- [ ] Кликнуть правой кнопкой на колонку E (Назначение)
- [ ] Выбрать "Вставить 1 столбец слева"
- [ ] Назвать новую колонку E: **"Этикетка ссылка"**
- [ ] Повторить: вставить еще 1 столбец слева
- [ ] Назвать новую колонку F: **"Этикетки инфа"**

**Результат:** Теперь у вас 21 колонка (A-U) вместо 19

**Старая структура:**
```
A: ID | B: ID-Карточки | C: ID-Чат | D: Наименование | E: Назначение | ...
```

**Новая структура:**
```
A: ID | B: ID-Карточки | C: ID-Чат | D: Наименование | E: Этикетка ссылка | F: Этикетки инфа | G: Назначение | ...
```

---

### 3. Проверить .env файл
```bash
# Обязательные переменные:
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./credentials.json
GOOGLE_DRIVE_FOLDER_ID=1rELo6cM69I8_105PY-XKuJv1W33VDH1R
GOOGLE_SHEET_ID=1JMzIi2-qNBwZsBqwDhOdJTocNjZ7BsdSXWs7cAKf3oU
JWT_SECRET=cosmetic-agent-jwt-secret-change-in-production

# Новое (нужно добавить):
GEMINI_API_KEY=ваш_ключ_здесь

# Опционально:
PORT=3000
NODE_ENV=development
```

---

### 4. Перезапустить сервер
```bash
# Остановить старый процесс
lsof -ti:3000 | xargs kill -9

# Запустить обновленный
node src/server.js
```

**Проверить в логах:**
```
🚀 Cosmetic Agent running on port 3000
📁 Google Drive Folder: 1rELo6cM69I8_105PY-XKuJv1W33VDH1R
📊 Google Sheet ID: 1JMzIi2-qNBwZsBqwDhOdJTocNjZ7BsdSXWs7cAKf3oU
🌍 Environment: development
```

**НЕ должно быть:**
```
⚠️ GEMINI_API_KEY is not set. AI features will be disabled.
```

---

## Опциональное тестирование (можно сделать ДО или ПОСЛЕ фронтенда)

### Тест 1: Создание карточки через API
```bash
# 1. Войти в систему через браузер http://localhost:3000
#    Скопировать токен из localStorage (F12 → Application → Local Storage → authToken)

# 2. Создать карточку
TOKEN="ваш_токен"

curl -X POST http://localhost:3000/api/cards/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "productName": "Тестовый продукт",
    "purpose": "Для теста",
    "application": "Тестируем"
  }'

# 3. Проверить:
#    - В ответе должен быть cardId: C-U2025_11_26_WF-XXXX-0001
#    - В Google Drive появилась папка пользователя
#    - В Google Sheets появилась строка с данными
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "cardId": "C-U2025_11_26_WF-0001-0001",
  "cardFolderId": "...",
  "userFolderId": "...",
  "photosFolderId": "...",
  "sheetRow": 5,
  "folderUrl": "https://drive.google.com/drive/folders/...",
  "message": "Карточка создана. Теперь можно загрузить этикетку."
}
```

---

### Тест 2: Загрузка этикетки с AI обработкой
```bash
# Используйте cardId и cardFolderId из предыдущего теста

curl -X POST "http://localhost:3000/api/cards/C-U2025_11_26_WF-0001-0001/label" \
  -H "Authorization: Bearer $TOKEN" \
  -F "labelFile=@/path/to/your/label.pdf" \
  -F "cardFolderId=ваш_folder_id" \
  -F "productName=Тестовый продукт"

# Проверить:
#    - Файл загружен в Drive папку карточки
#    - В Sheets колонки E и F заполнены (если есть AI ключ)
```

---

## Что читать в новой сессии

### Начать с:
1. **NEXT_SESSION.md** (5 минут) - быстрая инструкция
2. **IMPLEMENTATION_STATUS.md** (15-20 минут) - полная документация

### Для справки:
- `src/routes/cards.js` - примеры API запросов/ответов
- `src/services/aiService.js` - методы AI обработки
- `.claude/plans/greedy-bubbling-scone.md` - общий план

---

## Главная задача для новой сессии

### Phase 4: Frontend Restructuring

**Файлы для изменения:**
- `public/index.html` - переделать форму
- `public/app.js` - создать новый файл

**Что делать:**
1. Удалить поля Email и Имя (они в шапке)
2. Создать 5 блоков с прогрессивным раскрытием
3. Интегрировать с новым API
4. Реализовать вставку из буфера (Ctrl+V)
5. Добавить табы для переключения языков

**Ожидаемый результат:**
- Пользователь создает карточку → появляется блок этикетки
- Загружает этикетку → AI обработка → результаты → появляется блок INCI
- Загружает INCI → AI обработка → 3 блока результатов → появляется блок фото
- Загружает фото → готово

---

## Контакты для помощи

**Если что-то не работает:**
1. Проверить логи сервера в терминале
2. Проверить консоль браузера (F12 → Console)
3. Проверить Network tab (F12 → Network) для API запросов
4. Прочитать IMPLEMENTATION_STATUS.md раздел "Известные ограничения"

**Типичные проблемы:**
- 401 Unauthorized → токен истек, войти заново
- AI returns mock data → нет GEMINI_API_KEY
- Folder not found → проверить GOOGLE_DRIVE_FOLDER_ID
- Sheet error → проверить что добавлены колонки E и F

---

## Финальный чек-лист готовности

- [ ] ✅ GEMINI_API_KEY добавлен в .env
- [ ] ✅ Google Sheets: добавлены колонки E и F
- [ ] ✅ Сервер перезапущен
- [ ] ✅ В логах нет warning про GEMINI_API_KEY
- [ ] ✅ Прочитан NEXT_SESSION.md
- [ ] ✅ Готов начать Phase 4

---

**Статус:** Backend 100% → Frontend 0% → Let's go! 🚀
