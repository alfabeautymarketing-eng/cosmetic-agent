# Быстрый старт

## 📋 Что уже сделано

- ✅ Создана структура проекта
- ✅ Установлены все зависимости
- ✅ Настроен Express сервер
- ✅ Реализована интеграция с Google Drive
- ✅ Реализована интеграция с Google Sheets
- ✅ Создана система промптов для AI (заготовка)
- ✅ Инициализирован Git репозиторий

## 🚀 Следующие шаги

### Шаг 1: Настроить Google Service Account

Следуй инструкции в файле [SETUP_GOOGLE.md](./SETUP_GOOGLE.md)

Краткая версия:
1. Создай Service Account в Google Cloud Console
2. Включи Google Drive API и Google Sheets API
3. Скачай JSON ключ
4. Сохрани его как `credentials.json` в корне проекта
5. Дай Service Account доступ к папке Drive и таблице Sheets

### Шаг 2: Обновить .env файл

Открой файл `.env` и обнови:

```env
# ID твоей Google таблицы (найди в URL таблицы)
GOOGLE_SHEET_ID=твой_sheet_id_здесь
```

### Шаг 3: Создать структуру Google Sheets

Открой твою Google таблицу и создай лист с названием **"Карточки"** (или измени название в коде).

Добавь заголовки в первую строку:

| A | B | C | D | E | F | G | H | I | J | K | L | M | N |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ID | ID-Карточки | ID-Чат | Наименование | Назначение | Применение | ИНСИ | ИНСИ док | Активные ингредиенты без % | Активные ингредиенты без %Англ | состав БУКЛЕТ | состав БУКЛЕТ англ | Полный состав | Полный состав англ |

### Шаг 4: Запустить сервер

```bash
# Development режим (с автоперезагрузкой)
npm run dev

# Или production режим
npm start
```

Если все настроено правильно, увидишь:

```
✅ Google API авторизация успешна
🚀 Cosmetic Agent running on port 3000
📁 Google Drive Folder: 1rELo6cM69I8_105PY-XKuJv1W33VDH1R
📊 Google Sheet ID: твой_sheet_id
🌍 Environment: development
```

### Шаг 5: Протестировать webhook

В другом терминале (пока сервер работает):

```bash
node test-webhook.js
```

Это отправит тестовый запрос на сервер. Если все работает, в Google Drive создастся папка с тестовыми файлами, а в Google Sheets добавится новая строка.

## 🔧 Структура проекта

```
cosmetic-agent/
├── .env                           ← Переменные окружения (НЕ коммитится)
├── .env.example                   ← Пример переменных окружения
├── .gitignore                     ← Игнорируемые файлы
├── credentials.json               ← Google Service Account ключ (НЕ коммитится)
├── package.json                   ← Зависимости проекта
├── README.md                      ← Основная документация
├── SETUP_GOOGLE.md                ← Инструкция по настройке Google
├── QUICK_START.md                 ← Этот файл
├── test-webhook.js                ← Тестовый скрипт
│
└── src/
    ├── server.js                  ← Точка входа Express сервера
    │
    ├── config/
    │   └── googleAuth.js          ← Авторизация Google API
    │
    ├── routes/
    │   ├── health.js              ← Health check endpoint
    │   └── webhook.js             ← Webhook для n8n
    │
    ├── services/
    │   ├── cardProcessor.js       ← Основная логика обработки карточек
    │   ├── driveService.js        ← Работа с Google Drive
    │   └── sheetsService.js       ← Работа с Google Sheets
    │
    ├── utils/
    │   └── fileDownloader.js      ← Утилита для скачивания файлов
    │
    └── ai/                        ← Модуль AI (заготовка для будущего)
        ├── README.md              ← Документация AI модуля
        └── prompts.js             ← Промпты для AI обработки
```

## 📡 API Endpoints

### GET /health
Проверка состояния сервера

```bash
curl http://localhost:3000/health
```

Ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-01-18T12:00:00.000Z",
  "uptime": 123.456,
  "environment": "development"
}
```

### POST /webhook
Прием данных от n8n

```bash
curl -X POST http://localhost:3000/webhook \
  -H "Content-Type: application/json" \
  -H "x-webhook-secret: my-secret-key-123" \
  -d '{
    "chatId": "123456789",
    "productName": "Крем увлажняющий",
    "purpose": "Увлажнение кожи",
    "application": "Наносить на чистую кожу",
    "inci": "Aqua, Glycerin, Hyaluronic Acid",
    "inciDocUrl": "https://example.com/inci.pdf",
    "photoUrls": ["https://example.com/photo.jpg"]
  }'
```

Ответ:
```json
{
  "success": true,
  "cardId": "CARD-1234567890-123",
  "driveFolder": {
    "id": "folder_id_here",
    "url": "https://drive.google.com/drive/folders/...",
    "uploadedFiles": [
      { "name": "INCI.pdf", "id": "file_id" },
      { "name": "photo_1.jpg", "id": "file_id" }
    ]
  },
  "sheetRow": 2,
  "message": "Карточка успешно создана"
}
```

## 🔗 Интеграция с n8n

### 1. Запусти агента на Render

Следуй инструкции в [README.md](./README.md) раздел "Деплой на Render"

### 2. Настрой n8n workflow

В твоем n8n workflow:

1. **Telegram Trigger** → получает сообщения
2. **HTTP Request** → отправляет данные на агента

Пример HTTP Request node:

```
Method: POST
URL: https://your-agent.onrender.com/webhook
Headers:
  Content-Type: application/json
  x-webhook-secret: my-secret-key-123
Body:
{
  "chatId": "{{ $json.message.chat.id }}",
  "productName": "{{ $json.product_name }}",
  "purpose": "{{ $json.purpose }}",
  "application": "{{ $json.application }}",
  "inci": "{{ $json.inci }}",
  "inciDocUrl": "{{ $json.inci_doc_url }}",
  "photoUrls": {{ $json.photo_urls }}
}
```

## 🐛 Отладка

### Проблема: "Error: ENOENT: no such file or directory"

Решение: Убедись, что `credentials.json` находится в корне проекта

### Проблема: "Error 403: Forbidden"

Решение:
1. Проверь, что Service Account имеет доступ к папке Drive
2. Проверь, что Service Account имеет доступ к таблице Sheets
3. Проверь, что API включены в Google Cloud Console

### Проблема: Сервер не запускается

Решение:
1. Проверь, что порт 3000 свободен: `lsof -i :3000`
2. Проверь переменные окружения в `.env`
3. Проверь логи: сервер выводит детальную информацию об ошибках

## 📝 Логирование

Сервер выводит подробные логи:

```
[2024-01-18T12:00:00.000Z] POST /webhook
📨 Получен webhook от n8n: { ... }
🔄 Начинаем обработку карточки CARD-1234567890-123
📁 Папка создана: [CARD-1234567890-123] Крем увлажняющий (folder_id)
📄 INCI документ загружен
🖼 Фото 1 загружено
📊 Запись добавлена в Google Sheets: строка 2
✅ Карточка обработана успешно: CARD-1234567890-123
```

## 🎯 Следующие задачи

1. ☑️ Протестировать создание карточки
2. ☐ Добавить AI обработку (см. [src/ai/README.md](./src/ai/README.md))
3. ☐ Деплой на Render
4. ☐ Подключить к n8n
5. ☐ Протестировать end-to-end через Telegram

## 💡 Полезные команды

```bash
# Запустить в dev режиме
npm run dev

# Запустить в production
npm start

# Тестировать webhook
node test-webhook.js

# Проверить логи
# (в dev режиме логи выводятся в консоль)

# Проверить статус Git
git status

# Закоммитить изменения
git add .
git commit -m "Initial commit"
```

## 📞 Помощь

Если что-то не работает:

1. Проверь все шаги в этом файле
2. Проверь переменные окружения в `.env`
3. Проверь логи сервера
4. Проверь доступы Google Service Account

Удачи! 🚀
